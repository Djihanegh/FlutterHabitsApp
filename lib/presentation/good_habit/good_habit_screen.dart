import 'package:flutter/material.dart';

import 'components/body.dart';

class GoodHabitScreen extends StatelessWidget {
  const GoodHabitScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
